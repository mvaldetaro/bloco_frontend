import template from './contact-detail.html';

let contactDetailComponent = {
  bindings: {
    contato: '< contato',
    deletarContato: '&'
  },
  template
};

export default contactDetailComponent;
